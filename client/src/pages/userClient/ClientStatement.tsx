/* eslint-disable react-refresh/only-export-components */

const ClientStatement = () => {
  // Date, transaction desc, invoice numb, invoice amt, payment amt
  return <div>ClientStatement</div>;
};

export default ClientStatement;

export const loader = () => {
  return null;
};
